exports.run = async (client, message, args) => { // eslint-disable-line no-unused-vars
  message.reply("Invite Saya ke server: <https://shortid.co/tVDmI>");
};

exports.aliases = ["invite", "inv", "invt"];